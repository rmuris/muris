# Copy to .env and fill in.
DATABASE_URL="file:./dev.db"

# Optional. Without it the assistant runs in offline mode (local reasoning core,
# no Claude API calls). Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=""
